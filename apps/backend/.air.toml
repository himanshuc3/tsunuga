root = "."
tmp_dir = "tmp"

[build]
    cmd = "go build -o ./tmp/tango ./cmd/tango"
    bin = "./tmp/tango"
    include_ext = ["go"]
    exclude_ext = ["tmp", "vendor"]

[log]
    time = true

[misc]
    clean_on_exit = true